import App from "./components/App";

// webpack.config.js has this as the entry file
// when site is visited, it is sent here
// compiler imports App from components folder, running it
// the App component is then rendered in the index.html file (in templates/frontend)




//-----------------------Other iNfo-----------------------
// makes it so that Django renders a page that react can takeover 
// Django will make and manage the template and React will fill it in